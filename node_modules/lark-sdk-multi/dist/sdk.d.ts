import { PartnerConfig, SDKEvent, EventHandler, SDKMode } from './types';
declare class LoanEligibilitySDK {
    private config;
    private iframe?;
    private eventHandlers;
    private iframeUrl;
    private childWindow;
    private apiKey;
    private apiSecret;
    private sessionToken;
    private popupWindow;
    private containerId;
    constructor(config: PartnerConfig);
    initialize(config: PartnerConfig): Promise<void>;
    private validateConfig;
    private setupMessageListener;
    private setupSessionClearingListener;
    private handleSessionReinitialization;
    /**
     * Send logout message to larkfinserv-sdk iframe
     */
    private sendLogoutMessageToSDK;
    private generateSessionToken;
    private generateIframeUrl;
    openEligibilityCheck(mode: SDKMode): Promise<void>;
    private onClosePopupListener;
    closeFrame(): void;
    on(event: SDKEvent['type'], handler: EventHandler): void;
    off(event: SDKEvent['type'], _handler: EventHandler): void;
    sendData(data: Record<string, unknown>): void;
    private emitEvent;
    private handleError;
    private handleCloseMessage;
    private getClientIP;
    private getDeviceFingerprint;
    testConsentSubmission(): Promise<void>;
    /**
     * Check for existing journey for a phone number
     */
    /**
     * Complete a journey step
     */
    completeJourneyStep(journeyStep: string, stepData?: any): Promise<void>;
    private submitConsentData;
    /**
     * Manually trigger session clearing
     */
    clearSession(): void;
    /**
     * Get current session information
     */
    getSessionInfo(): any;
    /**
     * Reinitialize SDK with mobile number from localStorage
     */
    reinitializeWithMobileNumber(): Promise<void>;
    /**
     * Test function to manually trigger logout message (for testing)
     */
    testLogoutMessage(): void;
    /**
     * Test function to check userId mapping (for testing)
     */
    testUserIdMapping(): void;
    /**
     * Clear SDK data directly as a fallback when iframe communication fails
     */
    private clearSDKDataDirectly;
}
export default LoanEligibilitySDK;
